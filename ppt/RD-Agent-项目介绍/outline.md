# RD-Agent 项目介绍 PPT · 制作提纲（9 页 · v2.1）

## 页面目录（P1-P9）

| # | 页标题 | 核心目的 |
|---|---|---|
| P1 | 项目概述与架构 | 一页看懂"我们是谁、能干啥、怎么干、用了几个 LLM" |
| P2 | 成果与对标 | 30 秒说服评委"项目做完了，有数字有图" |
| P3 | 假设生成智能体 | 讲清 loop 起点"研究假设从哪来"，分 4 个入口场景讲差异 |
| P4 | 编程智能体 CoSTEER | 技术重头戏，讲编码循环 + B1~B5 论文代码还原能力 |
| P5 | 知识库全景 | 紧跟 CoSTEER，讲清"经验怎么沉淀、下次怎么复用" |
| P6 | 回测执行 + 反馈总结 | 闭环收尾：跑出来、总结好、沉淀进下一轮 |
| P7 | 多 LLM 差异化路由 | 产品亮点 + 成本逻辑：先讲 token 花在哪，再讲怎么降本 |
| P8 | 应用与商业价值 | 客群 / 成本对比 / 商业化路径 |
| P9 | 创新点 + 论文 + 路线图 | 收尾：创新是什么、论文在哪、下一步做什么 |

---

## 项目特点（P1 展开）

- **多智能体**：5 个角色（HypothesisGen / Developer / Runner / Summarizer / H2E）串成完整 R&D Loop。
- **自然语言策略生成因子**：用户用一句话描述研究想法，系统自动生成可回测因子/模型/策略。
- **多 LLM 分步路由**：每个 phase 独立选模型，不绑定单一厂商。

---



## P1 · 项目概述与整体架构（1 页）

### 一句话目标
让非技术评委在 60 秒内看懂：**"这是一个让多个 AI 智能体、多个大模型协作自动做量化研究的平台，覆盖 4 类研究任务，有一个完整的闭环循环。"**

### 页面布局（三栏/上下结构）
```
┌─────────────────────────────────────────────────────────────┐
│ [KICKER] RD-Agent · 项目总览                                │
│                                                             │
│  主标题：多智能体自动化量化研究平台                            │
│  Lead：让大模型代替研究员完成"提假设→写代码→回测→反馈"       │
│        的完整 R&D 循环，自动挖因子、迭代模型、沉淀经验。        │
│                                                             │
│  ┌─ 左：4 大场景入口 ─┐  ┌─ 中：5 智能体 RDLoop ─┐  ┌─ 右：多 LLM 映射 ─┐
│  │                    │  │                      │  │                  │
│  │ (见下方详表)       │  │ (5 节点流程图)        │  │ (4 phase ×       │
│  │                    │  │                      │  │  模型选择矩阵)    │
│  └────────────────────┘  └──────────────────────┘  └──────────────────┘
│                                                             │
│  底部标签云：5-Role RDLoop · CoSTEER V2 · 多 LLM 路由 · 4 层 KB · NeurIPS'25 │
└─────────────────────────────────────────────────────────────┘
```

### 左栏 · 4 大场景入口（每个场景一行，图标+名称+一句话说明）

| 图标色 | 场景名（CLI 命令） | 做什么 | 输入 | 输出 | 默认假设生成方式 |
|---|---|---|---|---|---|
| 🔵 蓝 | **因子研究** `qlib_factor` | 挖价量/基本面因子 | 自然语言研究方向 | 可交易因子代码 + IC/IR | Bandit/LinTS 自动选 |
| 🟣 紫 | **模型研究** `qlib_model` | 迭代预测模型（GNN/MLP/Transformer） | 模型架构方向 | 模型代码 + 回测指标 | Bandit/LinTS 自动选 |
| 🟡 金 | **组合策略** `qlib_quant` | 因子+模型+权重一起优化 | 投资目标/约束 | 完整策略组合 | LLM 主导（约束复杂） |
| 🟢 绿 | **研报因子抽取** `factor_from_report` | 从券商研报 PDF 中提取可回测因子 | PDF 研报 | 因子代码（复现研报结论） | 从研报内容直接生成假设 |

📖 引用：`docs/quickstart/Core.md`、`rdagent/app/cli.py`（4 个入口 main 函数）
🔧 代码锚点：
- [rdagent/app/qlib_rd_loop/factor.py](file:///workspace/rdagent/app/qlib_rd_loop/factor.py)
- [rdagent/app/qlib_rd_loop/model.py](file:///workspace/rdagent/app/qlib_rd_loop/model.py)
- [rdagent/app/qlib_rd_loop/quant.py](file:///workspace/rdagent/app/qlib_rd_loop/quant.py)
- [rdagent/app/qlib_rd_loop/factor_from_report.py](file:///workspace/rdagent/app/qlib_rd_loop/factor_from_report.py)

🟡 待补充：4 个场景各跑了多少任务（如有统计）

### 中栏 · 5 智能体 RDLoop 流程图（横排 5 节点）
```
HypothesisGen → Developer → Runner → Summarizer → (H2E 反馈回 HypothesisGen)
   (假设)        (编码)     (回测)     (总结)          ↻ 循环
```
每个节点下方一行字描述职责（详见下"5 智能体描述"）。H2E 节点用紫色表示"反馈专用"。箭头最终用 ↺ 回到 HypothesisGen。

#### 5 个智能体职责描述（给 PPT 制作人员 / 主讲人）
| 智能体 | 类比 | 一句话职责 | 输入 | 输出 | 主要调用的能力 |
|---|---|---|---|---|---|
| **① HypothesisGen**（假设生成） | 金工分析师 / 策略分析师 | 决定"这一轮研究什么" | 历史 Trace、SOTA 结果、自然语言方向 | 结构化 Hypothesis 对象（类型/目标/数据集/实验配置） | LLM / Random / **Bandit(LinTS)** 三种策略 |
| **② Developer**（编码） | 资深量化工程师 | 把假设写成可运行代码 | Hypothesis、V2 KB 检索结果 | Python 代码（因子/模型/策略） | CoSTEER 6 步演化循环、V2 三连查 |
| **③ Runner**（执行回测） | 回测平台 | 在回测平台中运行代码，执行回测分析，产出硬指标 | 代码、数据集配置 | IC/IR/年化/夏普 + 日志 | Docker/qlib backtest、SOTA 因子继承 |
| **④ Summarizer**（反馈总结） | 量化评审 / 绩效归因 | 判断"跑得好不好，下一步怎么改" | Runner 结果、当前 SOTA | 4 维反馈（可执行/信号/对比/建议）、新 Trace | LLM 总结、Trace 持久化 |
| **⑤ H2E**（假设→实验反馈） | 假设验证官 | 把本轮实验结果反馈到下一轮假设生成：上一轮假设是否成立、下一轮该朝哪个方向改进 | Summarizer 反馈 + 历史 Trace | 下一轮 Hypothesis 的输入信号、假设层面的 go/no-go 判断 | LLM（把执行层反馈升维到假设层） |

**H2E 的作用补充**：不是简单翻译，而是连接"实验层"和"研究层"。它读 Summarizer 的 4 维反馈，判断这个研究方向是否继续、是否换方向、是否调参复用，并把结论写回下一轮 HypothesisGen 的上下文。

🔧 代码锚点：
- 主循环：[rdagent/components/workflow/rd_loop.py](file:///workspace/rdagent/components/workflow/rd_loop.py)
- 契约基类：[rdagent/core/proposal.py](file:///workspace/rdagent/core/proposal.py)（HypothesisGen）、[rdagent/core/evolving_agent.py](file:///workspace/rdagent/core/evolving_agent.py)（Developer）

### 右栏 · 多 LLM 映射特性（这是 P1 必须突出的差异化点）
标题：**多 LLM 分步路由 · 不绑定单一模型**

内容：一个 4 行小表（不展开，P7 有专门页详细讲）：

| Phase | 可配模型类型 |
|---|---|
| HypothesisGen | 通用推理模型（如 glm-5.2） |
| Developer | **Coding 专精模型**（kimi-k2.7-code、deepseek-v4-flash） |
| Summarizer | 轻量总结模型（便宜即可） |
| H2E | 轻量映射模型 |

可配置模型类型的原因：不同 phase 任务难度和对模型能力的要求差异很大。编码 phase 最吃模型能力、token 消耗最大，必须用 coding 专精模型；总结和映射 phase 对推理深度要求低，用便宜模型即可；这样就可以按任务选模型，避免所有 phase 都用最贵的模型，显著降低成本。

下方一行：
- 任意 OpenAI 兼容端点都可接（火山方舟/Coding Plan/本地部署）
- **一键测试按钮**：不消耗正式 run token，独立验证模型连通性
- Prefect 缓存：相同 prompt 命中即 ¥0

📖 引用：`web/src/multialpha/use-multialpha.ts`（分步路由）、`rdagent/oai/backend/`（多后端）
🟡 待补充：当前默认用哪 4 个模型（填到表格里）

### 底部标签云
`5-Role RDLoop` · `CoSTEER V2` · `多 LLM 路由` · `4 层知识库` · `NeurIPS'25` · `Bandit` · `Paper-to-Code Benchmark`

💡 讲述要点（60 秒）：
> "评委好，这是 RD-Agent。左边是我们覆盖的 4 个量化研究场景，从挖因子到研报抽取都能做；中间是核心循环——5 个智能体像一个量化团队一样分工协作：研究总监提假设、工程师写代码、回测平台跑结果、评审会给反馈；右边是我们和其他项目最大的不同——**每个环节可以独立选不同的 LLM**，编码用 coding 专精模型，总结用便宜模型，不绑死任何一家。下面我从成果说起。"

✅ **交付物清单（做完 P1 后核对）**：
- [ ] Kicker："RD-AGENT · 项目总览"（蓝色大写）
- [ ] 主标题："多智能体自动化量化研究平台"
- [ ] Lead 一句话："让大模型代替研究员完成'提假设→写代码→回测→反馈'完整 R&D 循环..."
- [ ] 左栏：4 个场景入口卡片（蓝/紫/金/绿 4 色区分），每个含图标色点+名称+CLI命令+一句话说明
- [ ] 中栏：5 智能体横排流程图（HypothesisGen→Developer→Runner→Summarizer→H2E↺），每个节点有标签+名称+一句话职责
- [ ] 右栏（重点突出）："多 LLM 分步路由"面板，4 行小表（HypothesisGen/Developer/Summarizer/H2E 各配什么模型）+ 可配置原因说明，下方 3 个标签（任意OpenAI端点/一键测试/Prefect缓存）
- [ ] 底部标签云：7 个标签
- [ ] 页码：01 / 09（右上角）

---

## P2 · 成果与对标（1 页）⭐ 钩子页

### 一句话目标
用**大数字 + 真实截图**告诉评委："项目做完了，跑通了，有硬指标，有论文。"

### 页面布局
```
┌─────────────────────────────────────────────────────────────┐
│ [KICKER] 成果 · 与 SOTA 对标                                │
│                                                             │
│  主标题：不是 PPT 项目——我们跑出来了                          │
│  Lead：端到端量化流水线已落地，论文被 NeurIPS 2025 录用。     │
│                                                             │
│  ┌─ 上半：4 个大数字卡片 ──────────────────────────────────┐ │
│  │  [N 个任务]  [__% 成功率]  [NeurIPS'25]  [+__% IC]    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌─ 下半左：真实截图轮播区（2-3 张截图拼图）──────────────┐ │
│  │                                                        │ │
│  │  [产品 UI 截图]  [回测指标截图]  [Trace 时序截图]       │ │
│  │                                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌─ 下半右：论文列表 + 关键效率指标 ─────────────────────┐ │
│  │  • 6 篇核心论文标签                                    │ │
│  │  • 单任务 loop 数 / 收敛时间 / SOTA 因子数 / KB 节点数 │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 上半 · 4 个大数字卡片（等宽 4 栏）
| 卡片 | 颜色 | 数字 | 单位/副标 |
|---|---|---|---|
| 累计任务 | 蓝（主色） | 🟡 __ | 个因子/模型任务 |
| V2 编码成功率 | 金（强调） | 🟡 __% | V1 = __%，提升 __pp |
| 学术认可 | 粉（NeurIPS） | NeurIPS'25 | R&D-Agent-Quant 录用 |
| 效果提升 | 绿（成功） | +🟡 __% | IC vs Alpha158+Linear 基线 |

### 下半左 · 🖼 **必需要的 3 张截图（重要！）**
这一页必须有真实产品截图，否则像 PPT 项目。请按优先级准备：

| 优先级 | 截图内容 | 截哪里 | 用途 |
|---|---|---|---|
| 🔴 P0 | **产品 UI 主界面**（任务列表 + 任务创建按钮 + 侧边栏 trace 列表） | `web/` 前端启动后主页面 `/multialpha` 或主工作台 | 证明"这是个真产品，不是 notebook" |
| 🔴 P0 | **一次真实任务的 Trace 详情页**（可见 Hypothesis→Code→Run→Feedback 的完整时序，能看到 IC/IR 数字） | 点进某个 trace 的详情页 | 证明"真跑过，有真实数据" |
| 🟡 P1 | **回测结果指标面板**（IC/IR/年化/夏普 4 个指标的卡片或图表，最好有 vs SOTA 对比） | 任务详情中的 result 面板 | 证明"效果硬指标" |
| 🟡 P1（可选） | **CoSTEER 演化过程的终端输出截图**（能看到"轮次 1/10 失败→轮次 2 查 KB→轮次 5 成功"的日志） | 跑任务时的 terminal | 证明"多轮迭代真的在发生" |
| 🟢 P2（可选） | **模型选择 UI 截图**（4 个 phase 下拉选模型 + 测试按钮） | web 设置页 | 给 P7 预留复用 |

🟡 **行动项**：请你启动前端跑一个真实任务，截上述 3 张 P0 截图发我，或者告诉我截图路径。

### 下半右 · 论文列表 + 关键效率指标
**论文标签**：
- 蓝色：R&D-Agent (2505.14738)
- 紫色：R&D-Agent-Quant NeurIPS'25
- 蓝色：Data-Centric R&D (2404.11276)
- 紫色：CoSTEER (2407.18690)
- 蓝色：Multi-Agent Quant (2505.13172)
- 金色：LinTS ICML'13

下方小字补充：另有 B1~B5 五篇 GNN 模型作为 Paper-to-Code Benchmark（GPS / ViSNet / DiGNet / PMLP / LinkX）。

**关键效率指标**（4 行等宽数字）：
- 单任务平均 loop：🟡 __ 轮
- 平均收敛时间：🟡 __ 分钟
- SOTA 因子库：🟡 __ 条
- V2 知识图谱节点：🟡 __ 个

📖 引用：`docs/papers/README.md`、`docs/eval_results.md`（如有）
🟡 待补充：4 个大数字 + 4 个效率数字 + 3 张 P0 截图

💡 讲述要点（45 秒）：
> "先看成果。左上是我们跑过的任务量，右上是 CoSTEER V2 编码成功率提升，中间金色卡片是最硬的背书——NeurIPS 2025 录用，左下是 IC 相对基线的提升。左边几张截图是产品真实界面和一次任务的完整 Trace。下面我从 loop 的起点——假设生成讲起。"

### 🖼 **P2 截图获取指南（给项目负责人）**

请按以下步骤准备截图（这一页**必须有真实截图**，否则像 PPT 项目）：

| 优先级 | 截图名称 | 怎么截 | 截图应该包含什么 |
|---|---|---|---|
| 🔴 **P0-1 必选** | 产品 UI 主界面 | 1. 启动前端 `cd web && npm run dev`<br>2. 浏览器打开主页面（如 `/multialpha` 或工作台）<br>3. 确保页面有数据（跑过几个任务）<br>4. 截全屏（1920×1080 或窗口内主要区域） | ✅ 顶部导航栏<br>✅ 任务创建按钮/入口<br>✅ 侧边栏 trace 列表（能看到多个任务记录）<br>✅ 主内容区（任务卡片或列表） |
| 🔴 **P0-2 必选** | 真实任务 Trace 详情 | 1. 从侧边栏点进一个**已完成的、有结果的任务**<br>2. 确保能看到完整流程<br>3. 截该任务详情页 | ✅ Hypothesis（假设内容）<br>✅ Code（生成的代码片段或文件列表）<br>✅ Run（回测结果，有 IC/IR 数字！）<br>✅ Feedback（总结反馈）<br>✅ 时间线/步骤流转 |
| 🟡 P1 推荐 | 回测指标面板 | 在任务详情中，找到回测结果区域 | ✅ IC、IR、年化收益、夏普比率 4 个指标<br>✅ 最好能看到 vs SOTA 的对比<br>✅ 如有收益曲线图更好 |
| 🟡 P1 可选 | CoSTEER 演化终端日志 | 跑任务时的终端输出 | ✅ 能看到"轮次 1/10"、"轮次 2/5"这样的迭代过程<br>✅ 能看到"查 KB"、"error_query 命中"等日志<br>✅ 能看到最终成功的输出 |

> **截图注意事项**：
> - 截高清图（至少 1920px 宽）
> - 截之前把浏览器缩放调到 100%
> - 如页面有敏感信息（API Key、内部路径）请打码
> - 截图保存到 `ppt/RD-Agent-项目介绍/screenshots/` 目录下，命名如 `P0-1-main-ui.png`

✅ **交付物清单（做完 P2 后核对）**：
- [ ] Kicker："成果 · 与 SOTA 对标"
- [ ] 主标题："不是 PPT 项目——我们跑出来了"
- [ ] Lead："端到端量化流水线已落地..."
- [ ] 上半：4 个大数字卡片（等宽 4 栏），分别是累计任务数/V2成功率/NeurIPS'25/IC提升，数字用超大字号
- [ ] 下半左（2/3 宽度）：**3 张真实截图拼图**（P0-1 主界面、P0-2 Trace详情、P1 回测面板），用卡片框起来；如果还没截图，放占位框标注 `[P0 截图待补]`
- [ ] 下半右（1/3 宽度）：
  - [ ] 6 个论文标签（蓝/紫色区分）
  - [ ] "另有 B1~B5 五篇 GNN 模型..."小字说明
  - [ ] 紫色 accent-card：4 行效率指标（单任务loop/收敛时间/SOTA因子/KB节点）
- [ ] 页码：02 / 09
- [ ] **⚠️ 注意**：所有 `🟡 __` 占位符都填上真实数字后才能最终交付

---

## P3 · 假设生成智能体 HypothesisGen（1 页）

### 一句话目标
讲清"研究假设从哪来"，并**区分 4 个入口场景的不同处理逻辑**（因子、模型、组合、研报抽取各自的假设不一样）。

### 页面布局
```
┌─────────────────────────────────────────────────────────────┐
│ [KICKER] 智能体 ① · HypothesisGen                           │
│                                                             │
│  主标题：假设生成：不同入口，不同生成逻辑                      │
│  Lead：3 种通用策略（LLM/Random/Bandit）+ 按场景定制的假设模板│
│                                                             │
│  ┌─ 上半：3 种通用动作选择策略对比表 ─────────────────────┐ │
│  │  LLM / Random / Bandit(LinTS) 三列对比                 │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌─ 下半：4 个入口场景的假设差异（2×2 卡片）─────────────┐ │
│  │  ┌─因子研究─┐ ┌─模型研究─┐ ┌─组合策略─┐ ┌─研报抽取─┐ │ │
│  │  │         │ │         │ │         │ │         │ │ │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  底部 Bandit 小示意：双臂老虎机（因子臂 vs 模型臂）            │
└─────────────────────────────────────────────────────────────┘
```

### 上半 · 3 种通用策略对比表
| 策略 | 触发条件 | 逻辑 | 适用场景 | 代码锚点 |
|---|---|---|---|---|
| **LLM**（默认） | 默认配置 | 基于历史 Trace + 当前 SOTA，LLM 直接生成新假设 | 默认路径，质量最高 | [qlib/proposal/*.py](file:///workspace/rdagent/scenarios/qlib/proposal/) |
| **Random** | 知识库为空 / 冷启动 / 用户未给方向 | 在假设空间均匀采样；即使没有明确输入方向，Random 也能作为兜底探索策略，给系统一个起点 | 冷启动、探索新方向、所有入口场景无先验时均可触发 | [core/proposal.py](file:///workspace/rdagent/core/proposal.py) |
| **Bandit (LinTS)** | `action_selection=bandit` | 线性汤普森采样，把"研究因子/研究模型"当两个臂，按历史 IC 奖励更新后验 | 均衡探索-利用，长期自动分配精力 | [qlib/proposal/bandit.py](file:///workspace/rdagent/scenarios/qlib/proposal/bandit.py) `LinearThompsonTwoArm` |

📖 引用：`docs/papers/07-contextual-thompson-sampling.md`

### 下半 · 4 个入口场景的假设差异（2×2 卡片，每张卡片独立）

#### 卡片 1：因子研究（qlib_factor）
- **假设是什么**：一个"价量/基本面因子"的公式表达式
- **典型输入**：「试试换手率加权的动量背离」
- **假设对象字段**：`factor_formula`、`window`、`universe`（股票池）
- **评估标准**：IC、IR、Rank IC
- **特有机制**：SOTA 因子继承（新假设自动合并历史优秀因子集）
🔧 代码：[factor_proposal.py](file:///workspace/rdagent/scenarios/qlib/proposal/factor_proposal.py)

#### 卡片 2：模型研究（qlib_model）
- **假设是什么**：一个预测模型的架构/超参/训练策略
- **典型输入**：「试试用 Transformer 替换 LSTM，加 3 层注意力」
- **假设对象字段**：`model_cls`、`model_hyperparameters`、`training_config`
- **评估标准**：IC、MSE、回测收益（端到端）
- **特有机制**：CoSTEER 编码压力最大（模型代码复杂），V2 KB 命中收益最高；RAG 引导偏好 LSTM/GRU/MLP，暂不生成 GNN
🔧 代码：[model_proposal.py](file:///workspace/rdagent/scenarios/qlib/proposal/model_proposal.py)

#### 卡片 3：组合策略（qlib_quant）
- **假设是什么**：因子+模型+权重+风控约束的完整组合
- **典型输入**：「在中证 500 上用多因子模型，行业中性 + 5% 单股上限」
- **假设对象字段**：`alpha_list`、`model`、`risk_constraints`、`portfolio_method`
- **评估标准**：年化收益、夏普、最大回撤、换手率
- **特有机制**：LLM 主导（约束复杂，Bandit 难建模），需要 RAG 外挂查风控规则
🔧 代码：[quant_proposal.py](file:///workspace/rdagent/scenarios/qlib/proposal/quant_proposal.py)

#### 卡片 4：研报因子抽取（factor_from_report）
- **假设是什么**：从 PDF 研报中抽取的、可直接回测的因子公式
- **典型输入**：《XX 证券-量价因子之换手率背离》PDF
- **假设对象字段**：从 PDF 解析出的 `factor_formula`（无需 LLM 自由发挥）
- **评估标准**：复现 IC 是否接近研报声称的水平
- **特有机制**：**不经过 Bandit**，假设直接从研报内容抽取，假设空间由研报内容限定
🔧 代码：[factor_from_report.py](file:///workspace/rdagent/app/qlib_rd_loop/factor_from_report.py)、[qlib_rd_loop/conf.py:132](file:///workspace/rdagent/app/qlib_rd_loop/conf.py#L132)

🟡 待补充：每个场景的典型成功样例（一句话即可，如"在 300 个因子任务上..."）

💡 讲述要点（60 秒）：
> "假设生成是 loop 起点。3 种通用策略——默认 LLM 自由想、冷启动 Random 探索、长期跑用 Bandit 自动分配精力。**重点是右下角**——不同研究入口，假设的形状完全不同：因子场景假设是一个公式、模型场景是网络结构、组合场景是约束配置、研报场景是从 PDF 直接抽取。Bandit 机制出自 ICML 2013 的 LinTS 论文，把'研究因子还是研究模型'当两个老虎机臂，自动按收益分配。"

✅ **交付物清单（做完 P3 后核对）**：
- [ ] Kicker："智能体 ① · HypothesisGen"
- [ ] 主标题："假设生成：不同入口，不同生成逻辑"
- [ ] Lead："3 种通用策略 + 4 个入口场景定制的假设模板..."
- [ ] 上半：3 种通用策略对比表（LLM/Random/Bandit），Bandit 行金色高亮，标注"LinTS ICML 2013"
- [ ] 下半：2×2 卡片布局，4 个场景卡片
  - [ ] 因子研究：假设是公式，字段 factor_formula/window/universe，SOTA 继承机制
  - [ ] 模型研究：假设是网络结构，字段 model_cls/hyperparams，CoSTEER 压力最大
  - [ ] 组合策略：假设是完整组合，字段 alpha_list/risk_constraints，LLM 主导
  - [ ] 研报抽取：假设从 PDF 抽取，不经过 Bandit，字段直接从 PDF 解析
- [ ] 页码：03 / 09

---

## P4 · 编程智能体 CoSTEER Developer（1 页）⭐ 技术重头戏

### 一句话目标
讲清 CoSTEER 的 6 步演化循环和 V2 三连查，用成功率数字证明"多轮迭代 + 查知识库"真的有用。

### 页面布局
```
┌─────────────────────────────────────────────────────────────┐
│ [KICKER] 智能体 ② · CoSTEER Developer                       │
│                                                             │
│  主标题：编码智能体：像工程师一样多轮迭代                      │
│  Lead：LLM 一次写不对代码没关系——写、跑、看报错、查资料、改，  │
│        最多 10 轮，配合 V2 三连查，成功率收敛到 __%。         │
│                                                             │
│  ┌─ 上半：6 步循环主流水线（横排 6 个卡片）───────────────┐ │
│  │  Role → Retrieve → Think → Code → Debug → Evolve     │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌─ 下半左：V2 三连查（3 个卡片）────┐ ┌─ 下半右：B1~B5 还原率柱图 ─────────────┐ │
│  │  former_trace / component / error│ │  B1~B5 5 个 GNN 模型从零还原成功率      │ │
│  └──────────────────────────────────┘ └────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 上半 · 6 步循环（6 个等宽卡片，Retrieve 重点突出）
| # | 步骤名 | 做什么 |
|---|---|---|
| 01 | **Role** | System prompt 代入"资深量化工程师"角色 |
| 02 | **Retrieve** | 调 V2 三连查捞历史经验 |
| 03 | **Think** | Chain-of-Thought 推理改动方案 |
| 04 | **Code** | 生成/修改 Python 代码 |
| 05 | **Debug** | Evaluator 在 Docker 里跑，收集报错/指标 |
| 06 | **Evolve** | 失败则进入下一轮，成功则入库；最多 10 轮，兜底回退到最近可接受方案 |

🔧 代码锚点：
- [CoSTEER/evolving_strategy.py](file:///workspace/rdagent/components/coder/CoSTEER/evolving_strategy.py) 主循环
- [CoSTEER/evaluators.py](file:///workspace/rdagent/components/coder/CoSTEER/evaluators.py) 评估器

### 下半左 · V2 三连查（3 个卡片，每个卡片：名字 + 捞什么 + 作用）
| 查询 | 捞什么 | 人话作用 |
|---|---|---|
| **① former_trace** | 该任务最近 3 次失败痕迹 | **别再犯**——上次在这栽过，别这么写 |
| **② component** | 组件相似的成功实现 | **照着写**——别人写过类似组件，抄它的思路 |
| **③ error** | 同款报错的修复案例 | **照着修**——别人也踩过这个 error，用他的修法 |

🔧 代码锚点：[CoSTEER/knowledge_management.py](file:///workspace/rdagent/components/coder/CoSTEER/knowledge_management.py) `CoSTEERRAGStrategyV2.query()`

### 下半右 · B1~B5 Benchmark 还原率柱图
**B1~B5 Benchmark 还原率柱图**（5 根柱子，每根代表一个 GNN 模型从零还原成功率）
- B1 GPS：🟡 __%（★★★ 通用 Transformer）
- B2 ViSNet：🟡 __%（★★★★★ 最难，分子力场）
- B3 DiGNet：🟡 __%（★★★ 异亲和图）
- B4 PMLP：🟡 __%（★★ 路径 MLP，基线）
- B5 LinkX：🟡 __%（★★★★ 十亿边大图）

📖 引用：
- `docs/papers/04-costeer.md`（CoSTEER 论文 2407.18690）
- `docs/papers/benchmarks/`（B1~B5 5 篇 GT 论文）
- 已有单页 PPT：[ppt/CoSTEER机制详解/CoSTEER机制详解.html](file:///workspace/ppt/CoSTEER机制详解/CoSTEER机制详解.html)（可直接复用图）

🟡 待补充：
- B1~B5 每个的还原率百分比
- 平均轮数

💡 讲述要点（75 秒）：
> "这是我们最核心的技术模块 CoSTEER。LLM 一次写不对代码没关系，我们让它像工程师一样迭代：**代入角色→查资料→想→写→跑→改，最多 10 轮**。关键是第 2 步 Retrieve——V2 新加的三连查：别再犯、照着写、照着修，每次写码前先查知识库里的历史经验。下面是 B1~B5 五个 GNN 模型从零还原的成功率——连最难的 ViSNet 也能到 __%。这些经验存在哪？就是下一页讲的知识库。"

✅ **交付物清单（做完 P4 后核对）**：
- [ ] Kicker："智能体 ② · CoSTEER Developer"
- [ ] 主标题："编码智能体：像工程师一样多轮迭代"
- [ ] Lead："LLM 一次写不对没关系——写、跑、看报错、查资料、改..."
- [ ] 上半：6 步流水线横排卡片（Role→Retrieve→Think→Code→Debug→Evolve）
  - [ ] Retrieve 卡片重点突出
  - [ ] Evolve 卡片有循环箭头"↻ 最多 10 轮"
- [ ] 下半左（2/3 宽度）：
  - [ ] V2 三连查 3 个卡片（former_trace/component/error），每个有人话解释（别再犯/照着写/照着修）
- [ ] 下半右（1/3 宽度）：
  - [ ] B1~B5 GNN 还原率横向条形图（5 根柱子，按难度从易到难排）
  - [ ] B2 ViSNet 标注"最困难"
- [ ] 页码：04 / 09
- [ ] 🔧 可选：已有单页PPT `ppt/CoSTEER机制详解/CoSTEER机制详解.html` 可直接复用图

---

## P5 · 知识库全景（1 页）

> **紧跟 P4 之后**，因为 V2 知识图谱就是 CoSTEER 的"大脑记忆区"。

### 一句话目标
讲清"系统怎么沉淀经验"——V2 知识图谱为主，3 个配套知识库为辅。

### 页面布局（4 象限，V2 占左上大块）
```
┌─────────────────────────────────────────────────────────────┐
│ [KICKER] 四层知识库 · 让经验不丢失                           │
│                                                             │
│  主标题：4 层知识库协同：V2 图谱为主，3 个配套补充             │
│  Lead：项目内经验 + 跨项目文档 + 自定义源 + 量化 SOTA         │
│                                                             │
│  ┌─────────────────────────┬─────────────────────┐          │
│  │ ① CoSTEER V2 知识图谱   │ ② Context7 外部文档  │          │
│  │ （占 2 行高，蓝色主框）  │ （右上角小卡）        │          │
│  │   - 无向图               │  - timm/qlib/torch  │          │
│  │   - 三连查 former/       │  - 报错时自动查      │          │
│  │     component/error      │  - MCP 外挂         │          │
│  │   - 5 类节点             │                     │          │
│  ├─────────────────────────┼─────────────────────┤          │
│  │                          │ ③ RAG 通用外挂       │          │
│  │   （V2 框继续向下延伸）  │ ④ SOTA 知识库        │          │
│  │   - 跨会话积累           │ （右下 2 张小卡）    │          │
│  │   - 实验成功自动写入      │                     │          │
│  └─────────────────────────┴─────────────────────┘          │
│                                                             │
│  底部指标条：__ 图谱节点 · __ SOTA 因子 · __ Trace · 4 层协同│
└─────────────────────────────────────────────────────────────┘
```

### 4 个知识库详表（给制作人员）

| # | 名称 | 位置（代码） | 范围 | 触发时机 | 存储 | 核心作用 |
|---|---|---|---|---|---|---|
| **①** | **CoSTEER V2 知识图谱** | [CoSTEER/knowledge_management.py](file:///workspace/rdagent/components/coder/CoSTEER/knowledge_management.py) + [knowledge_management/graph.py](file:///workspace/rdagent/components/knowledge_management/graph.py) | 项目内，编码阶段 | 每轮编码前自动三连查 | `graph.pkl`（无向图 + 多个 dict） | 别再犯 / 照着写 / 照着修——让编码越跑越准 |
| ② | Context7 外部文档 | [components/agent/context7/](file:///workspace/rdagent/components/agent/context7/) | **跨项目**，查第三方库 | Evaluator 抛错时自动触发 | 外部 MCP server（本地搭建） | 查 timm/qlib/torch 官方文档和源码，找正确 API 用法 |
| ③ | RAG 通用外挂 | [components/agent/rag/](file:///workspace/rdagent/components/agent/rag/) | 任意 phase，自定义源 | 任意 phase 主动挂载 | 外部 MCP server | 接券商研报、会议纪要、自定义文档问答 |
| ④ | SOTA 知识库 | [core/knowledge_base.py](file:///workspace/rdagent/core/knowledge_base.py) + [log/sota_query.py](file:///workspace/rdagent/log/sota_query.py) | 项目内，量化经验 | Runner 跑完每个因子自动查同 model 下 SOTA 继承 | pickle 文件持久化 | 因子继承、实验反查、避免重造轮子 |

### CoSTEER V2 知识图谱的组成

V2 知识图谱是**无向图**——节点之间没有方向性，只表示"这个任务/组件/错误/成功实现之间有经验上的关联"。这样查询时可以灵活跳转：从当前任务 → 相似组件 → 成功案例，或从当前报错 → 同款报错 → 修复案例。

图中主要有 5 类节点（对应 5 类经验）：

| 节点类型 | 代表什么 | 在三连查中的作用 |
|---|---|---|
| **task_description** | 任务描述/目标 | 入口节点，先找到和当前任务相似的任务 |
| **component** | 任务拆分出的组件/子能力 | component 查询用：找到和当前任务共享组件的历史成功案例 |
| **task_trace** | 一次失败的实现尝试（代码+反馈） | former_trace 查询用：别再犯同样的错 |
| **task_success_implement** | 一次成功的实现（代码+反馈） | component/error 查询用：照着写 |
| **error** | 报错类型/错误信息 | error 查询用：照着修 |

节点之间的连接方式：
- `task_description` ↔ `component`：这个任务涉及哪些组件
- `task_description` ↔ `task_trace` / `task_success_implement`：这个任务有哪些失败/成功尝试
- `task_trace` ↔ `error`：这次失败报了什么错
- `task_success_implement` ↔ `error`：成功案例之前也踩过这个错，但后来修好了（用于 error 查询）

🔧 代码锚点：
- 三连查入口：[CoSTEER/knowledge_management.py#L431-L455](file:///workspace/rdagent/components/coder/CoSTEER/knowledge_management.py#L431-L455)
- 无向图实现：[components/knowledge_management/graph.py](file:///workspace/rdagent/components/knowledge_management/graph.py)
- 写入图谱：[CoSTEER/knowledge_management.py#L884-L924](file:///workspace/rdagent/components/coder/CoSTEER/knowledge_management.py#L884-L924)

### 4 个知识库之间的关系

- **V2 知识图谱是核心**：直接服务 CoSTEER 编码阶段，每轮必查，承载"自己项目里学出来的经验"。
- **Context7 是跨项目外援**：解决"第三方库 API 变了/用法不熟"导致的报错，不替代 V2。
- **RAG 通用外挂是扩展入口**：允许用户挂任何自定义文档（研报、内部规范），给其他 phase 用。
- **SOTA 知识库是量化专用经验**：服务 Runner 的"抄作业"和"去重"逻辑，和 V2 互补。

简言之：V2 负责"怎么把代码写对"，另外三个分别负责"查外部文档""接自定义源""记量化结果"。

📖 引用：`docs/papers/04-costeer.md`（V2 详述）
🟡 待补充：
- V2 图谱节点数（component/task/trace/impl/error 各多少）
- SOTA 因子库规模
- Trace 总数

💡 讲述要点（45 秒）：
> "这是让系统'越跑越聪明'的关键——4 层知识库。主力是左边大块 V2 知识图谱，每轮编码前自动三连查，实验成功自动写回。其他三个是配套：Context7 查别人家库的文档（比如 timm 新 API 怎么用）；RAG 外挂可以接券商研报自定义源；SOTA 知识库专门记量化经验，让新因子自动继承历史最优集。4 层协同，经验真的不丢。跑完代码就到回测和反馈。"

✅ **交付物清单（做完 P5 后核对）**：
- [ ] Kicker："四层知识库 · 让经验不丢失"
- [ ] 主标题："V2 知识图谱为主 · 3 个配套补充"
- [ ] Lead："项目内编码经验 + 跨项目三方库文档 + ..."
- [ ] 4 象限田字布局：
  - [ ] ① V2 知识图谱（左侧 2×1 大块，明显大于其他三个）：含标题、标签（项目内·编码阶段·每轮必查）、3 个要点（5类节点/三连查/自动写回）
  - [ ] ② Context7 外部文档（右上小卡）：跨项目·报错修复·MCP外挂
  - [ ] ③ RAG 通用外挂（右中小卡）：任意Phase·自定义文档源
  - [ ] ④ SOTA 知识库（右下小卡）：项目内·量化经验·pickle
- [ ] 底部关键指标条：V2图谱节点数 / SOTA因子 / Trace总数 / "4层协同"
- [ ] 页码：05 / 09
- [ ] **位置确认**：这页必须紧跟 P4 CoSTEER 之后！

---

## P6 · 回测执行 + 反馈总结（1 页）

### 一句话目标
讲清闭环的收尾——怎么跑、怎么判断好坏、怎么沉淀经验回传给下一轮。

### 页面布局
```
┌─────────────────────────────────────────────────────────────┐
│ [KICKER] 智能体 ③④ · Runner + Summarizer                    │
│                                                             │
│  主标题：闭环：真跑才知道好坏，反馈沉淀成经验                  │
│  Lead：Runner 实干（继承 SOTA → 去重 → 回测），Summarizer     │
│        当教练（4 维反馈），结果全部入 Trace。                 │
│                                                             │
│  ┌─ 左：Runner 职责 3 条 ──────┐ ┌─ 右：Summarizer 4 维反馈 ─┐ │
│  │  • SOTA 因子继承             │ │  ① 可执行性               │ │
│  │  • 因子去重/合并             │ │  ② 信号质量 IC/IR         │ │
│  │  • qlib 真实回测             │ │  ③ vs SOTA 对比           │ │
│  └─────────────────────────────┘ │  ④ 下一步自然语言建议     │ │
│                                  └──────────────────────────┘ │
│  ┌─ 下半：真实 Trace 时序示意（4 步，红/蓝/绿色条）────────┐ │
│  │  T0 红：失败 → T1 红：查错 → T2 蓝：修复 → T3 绿：入库 │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 左栏 · Runner 三大职责
1. **SOTA 继承**：跑新实验前自动"抄作业"——加载同 model 下历史最优因子集/模型超参作为基础，不从头造轮子（在因子场景最典型，模型场景也可复用历史模型配置）
2. **去重/合并**：因子场景下余弦相似度 > 80% 的因子自动合并，避免重复劳动
3. **真实回测**：调用 qlib 在 Docker 隔离环境中跑，产出 IC / IR / 年化 / 夏普 4 项硬指标

🔧 代码：`rdagent/components/runner/`

### 右栏 · Summarizer 4 维反馈
| 维度 | 判断什么 | 典型输出 |
|---|---|---|
| ① 代码可执行性 | 能不能跑 | "代码 shape mismatch，报错在第 23 行" |
| ② 信号质量 | IC/IR 是否达标 | "IC=0.05 低于阈值 0.06" |
| ③ vs SOTA 对比 | 是否超越当前最优 | "未超越 SOTA（IC=0.08），需要调整方向" |
| ④ 下一步建议 | 往哪改 | 自然语言："尝试缩短窗口到 10 日，并加入成交量加权" |

🔧 代码：`rdagent/components/feedback/`、[log/storage.py](file:///workspace/rdagent/log/storage.py) Trace 持久化

### 下半 · Trace 时序示例（4 行，带颜色条）
```
T0  [红-失败]  Hyp: 价量背离20d → Dev: factor.py → Run: IC=0.05 → Sum: 未达SOTA
T1  [红-报错]  Hyp: 动量反转 → Dev: 复用SOTA+新增 → Run: ❌ shape mismatch
T2  [蓝-迭代]  Dev: error_query命中修复 → Run: IC=0.07
T3  [绿-入库]  Run: IC=0.08, 年化+__% → Sum: 超越SOTA, 写入知识库 🎉
```

🖼 截图需求：
- 🔴 **P0**：从 `log/ui` 前端截一个真实 trace 的时间线详情页（最好能看到 T0→T3 这种演化过程）

🟡 待补充：
- 真实 trace 截图
- SOTA 因子数、Trace 总数

💡 讲述要点（45 秒）：
> "Runner 和 Summarizer 让系统真正闭环。Runner 先抄历史最优作业再跑，避免从零开始；Summarizer 从 4 个维度打分：能不能跑、信号好不好、比 SOTA 强不强、下一步怎么改。关键是——所有结果都写入 Trace 持久化，下一轮 HypothesisGen 和 CoSTEER 都能查，就不会白跑。下面讲我们另一个大差异化——多 LLM 路由。"

✅ **交付物清单（做完 P6 后核对）**：
- [ ] Kicker："智能体 ③④ · Runner + Summarizer"
- [ ] 主标题："闭环：真跑才知道好坏，反馈沉淀成经验"
- [ ] Lead："Runner 实干（抄 SOTA 作业 → 去重 → qlib 真实回测）..."
- [ ] 上半双栏布局：
  - [ ] 左栏 Runner 卡片（蓝色）：3 条职责（SOTA因子继承/因子去重合并/Docker+qlib真实回测）
  - [ ] 右栏 Summarizer 卡片（紫色）：4 维反馈（可执行性/信号质量/vs SOTA/下一步建议）
- [ ] 下半 Trace 时序示例（4 行）：
  - [ ] T0 红（失败）→ T1 红（报错）→ T2 蓝（迭代修复）→ T3 绿（成功入库🎉）
  - [ ] 每行左边有颜色竖线
  - [ ] 🔴 P0 可替换为真实前端 Trace 时间线截图
- [ ] 页码：06 / 09

---

## P7 · 多 LLM 差异化路由（1 页）⭐ 产品亮点 + Token 成本逻辑

> **把 token 饼图放在这里**——先告诉评委"token 花在哪"，紧接着告诉评委"我们怎么做按任务挑模型省钱"，逻辑闭环。

### 一句话目标
讲清"不绑死单一 LLM"的产品能力，并**用 token 占比饼图解释为什么多模型路由能降本**。

### 页面布局
```
┌─────────────────────────────────────────────────────────────┐
│ [KICKER] 多 LLM 差异化路由                                  │
│                                                             │
│  主标题：按任务挑模型——编码用 coding 专精模型，总结用便宜模型  │
│  Lead：4 个 phase 独立配模型 + 一键测试连通性 + Prefect 缓存 │
│                                                             │
│  ┌─ 左：Token 占比饼图/条形图 ──────┐ ┌─ 右：为什么要多模型 ─┐ │
│  │  Developer __% (最大头)          │ │  饼图解读：          │ │
│  │  HypothesisGen __%               │ │  Developer 吃__% token│ │
│  │  Summarizer __%                  │ │  → 必须用 coding 专精 │ │
│  │  H2E __%                         │ │  Summarizer 只__%   │ │
│  │  Runner 0% (不调LLM)             │ │  → 用便宜模型即可     │ │
│  └──────────────────────────────────┘ └─────────────────────┘ │
│                                                             │
│  ┌─ 下半：4 Phase × 模型矩阵 + 前端 2 个关键功能 ─────────┐ │
│  │  [模型选择矩阵] [可视化编辑器截图占位] [模型测试按钮截图]│ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  底部：单任务完整成本 ≈ ¥__ （Prefect 命中 ¥0）              │
└─────────────────────────────────────────────────────────────┘
```

### 左 · Token 消耗占比图（条形或环形饼图）
🟡 请你提供每个 phase 的 token 占比（%），按如下结构填：
| Phase | 占比 | 颜色 |
|---|---|---|
| Developer（CoSTEER） | 🟡 __%（预估 50%+） | 金色（最大头） |
| HypothesisGen | 🟡 __% | 蓝色 |
| Summarizer | 🟡 __% | 紫色 |
| H2E | 🟡 __% | 绿色 |
| Runner | 0%（不调 LLM） | 灰色 |

### 右 · "为什么要多模型"饼图解读（3 条 bullet）
- 🔑 **Developer 是 token 大户**（占 __%）→ 必须用 coding 专精模型（kimi-k2.7-code / deepseek-v4-flash），提升成功率等于直接降本
- 💡 Summarizer / H2E 是轻量任务 → 用便宜模型即可，没必要用最贵的
- ⚡ Runner 不调 LLM → 零 token 成本，回测算力是唯一开销

### 下半 · 4 Phase × 模型选择矩阵
| Phase | 默认模型 🟡 | 可选模型（火山方舟 Coding Plan） | 模型类型建议 |
|---|---|---|---|
| HypothesisGen | 🟡 | glm-5.2 / minimax-m3 / kimi-k2.7-code / deepseek-v4-flash | 通用推理 |
| Developer | 🟡 | 同上，推荐 coding 专精 | **Coding 专精** |
| Summarizer | 🟡 | 同上 | 轻量即可 |
| H2E | 🟡 | 同上 | 轻量即可 |

### 下半右 · 🖼 前端两个关键功能截图占位
1. **分步路由可视化编辑器**：UI 上 4 个下拉菜单，每个 phase 选模型（🟡 P0 截图）
2. **模型测试按钮**：每个模型旁边有"测试"按钮，不消耗正式 run token（🟡 P1 截图）

下方功能标签：
- 🔌 任意 OpenAI 兼容端点（火山方舟 / 本地部署 / 自托管 vLLM）
- 🧪 一键测试连通性（ping 请求验证密钥）
- 💾 Prefect 缓存命中 ¥0
- 🔄 运行时可热切换

🔧 代码锚点：
- 后端多模型：[rdagent/oai/backend/](file:///workspace/rdagent/oai/backend/)
- Agent 基类：[components/agent/base.py](file:///workspace/rdagent/components/agent/base.py) `enable_cache`
- 前端路由：`web/src/multialpha/use-multialpha.ts`

🟡 待补充：
- 各 phase 默认模型
- 每 phase token 占比百分比（**最关键**）
- 单任务完整成本 ¥__
- 🖼 前端模型选择 UI 截图

💡 讲述要点（60 秒）：
> "这是我们和大多数开源多智能体项目最大的不同——**不绑死单一模型**。先看左边饼图：token 花在哪？Developer 一个人吃了 __%，是最大头；Summarizer 和 H2E 加起来才 __%。那当然是——编码用最强的 coding 专精模型，总结用便宜的，这是朴素的成本优化。下面是模型选择矩阵，每个 phase 独立配，前端可视化下拉，旁边有测试按钮不耗正式 token，还有 Prefect 缓存命中就 ¥0。这样一套下来单任务成本控制在 ¥__。有了产品能力，下面聊商业价值。"

### 🖼 **P7 截图获取指南**
| 优先级 | 截图名称 | 怎么截 | 包含什么 |
|---|---|---|---|
| 🔴 **P0 必选** | 分步路由可视化编辑器 | 设置页/模型配置页 | 4个phase的下拉菜单，能看到每个phase选了什么模型 |
| 🟡 P1 推荐 | 模型测试按钮 | 模型选择区域附近 | "测试连通性"或类似按钮，点击后的反馈 |

✅ **交付物清单（做完 P7 后核对）**：
- [ ] Kicker："多 LLM 差异化路由 · 产品亮点"
- [ ] 主标题："按任务挑模型——编码用专精模型，总结用便宜模型"
- [ ] Lead："先看 token 花在哪，再看怎么挑模型降本..."
- [ ] 上半双栏：
  - [ ] 左栏：Token 消耗占比横向条形图（Developer 金色最大条，按占比从大到小排），底部标注"Runner 0%* 不调LLM"和"单任务总成本 ≈ ¥__"
  - [ ] 右栏（紫色 accent-card）：3条解读（Developer是大户必须用专精模型/Summarizer用便宜的/Runner零token）
- [ ] 下半双栏（左宽右窄）：
  - [ ] 左：4 Phase × 模型选择矩阵表（3列：Phase/默认模型/可选模型）
  - [ ] 右：2个前端功能卡片
    - [ ] ① 分步路由编辑器（🔴 P0 截图占位）
    - [ ] ② 模型一键测试按钮（🟡 P1 截图占位）
    - [ ] 底部4个标签：OpenAI兼容/MCP/Prefect缓存¥0/热切换
- [ ] 页码：07 / 09
- [ ] **逻辑确认**：这页必须先展示 token 花在哪，再讲怎么挑模型省钱——逻辑闭环！

---

## P8 · 应用与商业价值（1 页）

### 一句话目标
回答评委的"so what"——谁会用、省多少钱、怎么赚钱。

### 页面布局
```
┌─────────────────────────────────────────────────────────────┐
│ [KICKER] 应用场景 · 商业价值                                 │
│                                                             │
│  主标题：三类客群 · 一条朴素商业逻辑                          │
│  Lead：一个资深量化研究员年薪 50-100w，系统一年远低于此       │
│        还能 24h 不停跑。                                     │
│                                                             │
│  ┌─ 上：4 类客群卡片 ────────────────────────────────────┐ │
│  │  [券商自营/公募] [量化私募] [经纪业务客户] [教育/研究机构]│ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌─ 下左：单因子成本对比表 ──────┐ ┌─ 下右：3 条商业化路径 ─┐ │
│  │  人工 __天 / ¥__              │ │  ① SDK 授权（年订阅）  │ │
│  │  自动 __小时 / ¥__            │ │  ② SaaS（按token计费）│ │
│  │  降幅 __× / __%↓              │ │  ③ QRaaS（按产出分成） │ │
│  └──────────────────────────────┘ └───────────────────────┘ │
│                                                             │
│  POC 进展：🟡 如有合作机构请填                                 │
└─────────────────────────────────────────────────────────────┘
```

### 上半 · 4 类客群（4 个等宽卡片，不同颜色图标）
| 客群 | 颜色 | 痛点 | 我们的价值 |
|---|---|---|---|
| **券商自营 / 公募量化** | 蓝 | 研究员稀缺、年度人力成本 50-100w/人、合规要求内网部署 | 自动挖因子+回测+SOTA 继承，1 套系统顶 N 人，内网部署合规 |
| **量化私募** | 紫 | 模型更新慢、跨市场（A股/港美/加密）适配贵 | 双管线（因子+模型）+ Bandit 自动分配精力，24h 不停跑 |
| **经纪业务客户** | 绿 | 有自己的策略思路，但量化上手门槛高、不会写代码、不会回测 | 自然语言描述想法即可自动生成因子/策略并回测验证，降低参与门槛 |
| **量化教育 / 研究机构** | 金 | 论文复现难、教学成本高、学生难上手 | Paper-to-Code Benchmark 端到端可复现，教学/科研一键起 |

### 下左 · 单因子成本对比表（3 行表格）
| 方式 | 耗时 | 成本 |
|---|---|---|
| 人工研究员 | 🟡 __ 天 | ¥🟡 __（估） |
| RD-Agent 自动 | 🟡 __ 小时 | ¥🟡 __ |
| **降幅** | **🟡 __×** | **🟡 __%↓** |

🟡 待补充：
- 人工单因子耗时：建议问团队，一般资深研究员 3-5 天
- 人工单因子成本：按年薪 60w ÷ 250 工作日 ≈ ¥2400/天 估算
- 系统单因子耗时和成本：从真实跑的日志统计

### 下右 · 3 条商业化路径（按落地难度从易到难）
1. **① SDK 授权**（蓝色）——给机构内网部署，按席位年订阅；最易落地，合规友好
2. **② SaaS 平台**（紫色）——按 token / 按任务计费；降低小团队使用门槛
3. **③ QRaaS（量化研究即服务）**（金色）——按产出因子/策略实盘分成；天花板最高，但需要业绩背书

POC 进展：🟡 如有试点客户/合作机构，写"已与 __ 家机构沟通 POC"；没有就删这行。

💡 讲述要点（45 秒）：
> "朴素逻辑：一个资深量化研究员年薪 50-100w，我们一套系统远低于此还能 24h 跑。四类客群分别对应不同痛点：券商要合规内网、私募要跨市场效率、经纪业务客户有想法但不会写代码、教育/科研机构要可复现。左下是单因子的成本对比，人工几天几千块，我们几小时几块钱。三条商业化路径从易到难：先做授权，再做 SaaS，长期做按产出分成。理论上可以持续做因子/策略挖掘。"

✅ **交付物清单（做完 P8 后核对）**：
- [ ] Kicker："应用场景 · 商业价值"
- [ ] 主标题："四类客群 · 一条朴素商业逻辑"
- [ ] Lead："一个资深量化研究员年薪 50-100w..."
- [ ] 上半：4 个客群卡片（蓝/紫/绿/金），每个含图标编号+客群名+痛点（小字）+价值说明
  - [ ] 01 券商自营/公募（蓝）：合规内网、顶N人
  - [ ] 02 量化私募（紫）：跨市场、24h跑
  - [ ] 03 经纪业务客户（绿）：有策略思路但不会写代码，自然语言即可生成并回测
  - [ ] 04 教育/研究机构（金）：可复现、一键起
- [ ] 下半双栏：
  - [ ] 左：单因子成本对比表（3行：人工/自动/降幅），底部小字注释人工成本估算
  - [ ] 右：3条商业化路径卡片（蓝→紫→金按易到难），POC进展行（有则填，无则删）
- [ ] 页码：08 / 09

---

## P9 · 创新点 + 论文 + 路线图（1 页）收尾

### 一句话目标
评委离场前记住 3 件事：创新是什么、论文在哪、下一步做什么。

### 页面布局
```
┌─────────────────────────────────────────────────────────────┐
│ [KICKER] 创新 · 学术 · 路线图                                │
│                                                             │
│  ┌─ 左上：4 大创新点（2×2 带色条卡片）───────────────────┐ │
│  │  ①架构 ②方法 ③产品 ④评估                              │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌─ 右上：学术成果（NeurIPS 金色高亮）───────────────────┐ │
│  │  🏆 NeurIPS 2025 + 5+1 篇标签 + 在投                 │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌─ 下半：路线图（3 列：短期/中期/长期）─────────────────┐ │
│  │  [3个月]      [6个月]       [12个月]                  │ │
│  │  打磨核心能力   扩场景上平台   愿景：无人化 SaaS        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  底部：联系方式（GitHub / 文档 / 邮箱）                      │
└─────────────────────────────────────────────────────────────┘
```

### 左上 · 4 大创新点（2×2 卡片，每卡片左边彩色竖条）
| # | 颜色 | 类别 | 内容 |
|---|---|---|---|
| 01 | 蓝 | 架构创新 | 5 角色多智能体 R&D-Loop，把"研究→代码→回测"全自动串成端到端闭环 |
| 02 | 紫 | 方法创新 | CoSTEER V2 三连查知识图谱，编码成功率从 ~20% 提升到 🟡 __% |
| 03 | 金 | 产品创新 | 多 LLM 分步路由 + 模型测试按钮，不绑定单一厂商，按任务挑模型降本 |
| 04 | 绿 | 评估创新 | 端到端 Paper-to-Code Benchmark（B1~B5 五个 GNN 模型），可量化可复现 |

### 右上 · 学术成果
- **🏆 NeurIPS 2025（金色大字）**
  - R&D-Agent-Quant: Automating Quantitative Finance Research with LLM-based Multi-Agent System (arXiv:2505.15155)
  - 注：该论文为项目团队参与工作，R&D-Agent 系列为本项目基础参考与扩展来源。
- **论文标签云**（项目引用/扩展的论文）：
  - R&D-Agent (2505.14738)
  - Data-Centric R&D (2404.11276)
  - Multi-Agent Quant (2505.13172)
  - CoSTEER (2407.18690)
  - LinTS (ICML 2013)
- **Benchmark GT**：GPS、ViSNet、DiGNet、PMLP、LinkX（B1~B5）
- 在投/准备中：🟡 有就填

### 下半 · 路线图（3 列，不同颜色）
| 阶段 | 颜色 | 时间 | 主题 | 关键事项 |
|---|---|---|---|---|
| 短期 | 蓝 | 3 个月 | 打磨核心能力 | • B2 ViSNet 还原率 60%→75%+ <br> • Bandit 权重调优 / UCB 对比 <br> • 股池预测低延迟优化 <br> • Mem0 跨 KB 长期记忆上线 |
| 中期 | 紫 | 6 个月 | 扩场景·上平台 | • 多市场扩展（加密/港美股） <br> • 跨用户共享 SOTA KB <br> • 在线学习（每日新研报→自动挖因子） <br> • SaaS 产品打磨 |
| 长期 | 金 | 12 个月 | 愿景 | • 完全无人化量化研究 SaaS <br> • 论文→因子开放 Benchmark 平台 <br> • 跨团队联邦 RAG 共建 KB <br> • 扩展到 AI4Science 其他领域 |

### 底部 · 联系方式栏
- GitHub：🟡 填仓库地址
- 文档：🟡 填文档站地址
- 团队/邮箱：🟡 填联系方式
- 右下角标签：`Thank You · Q&A`

💡 讲述要点（45 秒收尾）：
> "最后一页总结。左边 4 个创新点涵盖架构、方法、产品、评估四个维度；右上是最硬的学术背书——NeurIPS 2025 录用；下方路线图：短期 3 个月打磨核心能力，中期扩场景上平台，长期愿景是无人化量化 SaaS。这是我们的项目，谢谢各位评委，欢迎交流。"

✅ **交付物清单（做完 P9 后核对）**：
- [ ] Kicker："创新 · 学术 · 路线图"
- [ ] 上半双栏：
  - [ ] 左上：4 大创新点（2×2卡片），每个左边彩色竖条（蓝/紫/金/绿），右上显示编号 INNOV·01~04
    - 01 架构创新（蓝）：5角色R&D-Loop端到端闭环
    - 02 方法创新（紫）：CoSTEER V2三连查，成功率20%→__%
    - 03 产品创新（金）：多LLM路由+测试按钮，不绑厂商
    - 04 评估创新（绿）：Paper-to-Code B1~B5 Benchmark
  - [ ] 右上（学术背书）：
    - NeurIPS 2025
    - 论文标题 + arXiv 号 + 归属说明
    - 5 个论文标签（项目引用/扩展）
    - "B1~B5 GT: GPS · ViSNet · DiGNet · PMLP · LinkX"
    - 在投论文（有则填）
- [ ] 下半：3列路线图（蓝/紫/金）
  - 短期·3个月（蓝）：打磨核心能力（4个bullet）
  - 中期·6个月（紫）：扩场景·上平台（4个bullet）
  - 长期·12个月（金）：愿景（4个bullet）
- [ ] 底部：
  - 左：关键指标条（9页/5智能体/4层KB/1篇NeurIPS）
  - 右：联系方式（GitHub/文档/邮箱）+ "Thank You · Q&A" 标签
- [ ] 页码：09 / 09
- [ ] 这是最后一页！讲完说"谢谢"，不要加多余内容

---

## 📦 最终交付检查清单（PPT全部做完后核对）

做完所有9页后，用这个清单做最终检查：

- [ ] 所有页面 16:9，1920×1080，深色背景 `#0d0d12`/`#161616`
- [ ] 每页都有：左上蓝色kicker、右上页码、标题下灰色lead
- [ ] 所有 `🟡 __` 粉色占位符都替换成了真实数字/文字
- [ ] 所有 `🖼` 截图占位都替换成了真实产品截图（无虚线框）
- [ ] P2 有 2-3 张真实产品截图（主界面/Trace详情/回测面板）
- [ ] P5 紧跟 P4，知识库在 CoSTEER 之后（逻辑顺：编码→存经验）
- [ ] P7 Token 条形图在多LLM路由页（逻辑顺：先看花在哪→再讲怎么省钱）
- [ ] P3 4个入口场景（因子/模型/组合/研报）区分清楚了
- [ ] P1 右栏多LLM映射是差异化亮点
- [ ] 按「💡 讲述要点」顺一遍，8-10分钟能讲完
- [ ] 导出PDF备份一份

---

## 🟡 所有待补充项汇总（一次填完清单）

### 数字类（优先补）
| 位置 | 内容 | 建议来源 |
|---|---|---|
| P2 | 累计完成任务数（因子+模型） | 查 `log/` 或数据库统计 |
| P2 | V1/V2 编码成功率 + 提升幅度 | CoSTEER 论文实验数据 |
| P2 | vs Alpha158+Linear 的 IC 提升 | R&D-Agent-Quant 论文实验 |
| P2 | 单任务平均 loop 数 / 收敛时间 | 跑一批任务统计 |
| P2/P5 | SOTA 因子数 / V2 图谱节点数 / Trace 总数 | 查 KB 和 log 存储 |
| P3 | Bandit vs LLM/Random 对比实验数字（如有） | 小实验 |
| P4 | B1~B5 每个模型的还原率% | Benchmark 实验结果 |
| P7 | 每 phase token 占比% | 你说已有统计 |
| P7 | 单任务完整成本 ¥ | 按 token 价格算 |
| P7 | 4 个 phase 各默认用什么模型 | 看 `.env` / `conf.py` |
| P8 | 人工 vs 自动单因子耗时/成本 | 团队估算+真实日志 |

### 截图类（按优先级）
| 优先级 | 位置 | 截图内容 |
|---|---|---|
| 🔴 P0 | P2 | 产品 UI 主界面（任务列表+侧边栏） |
| 🔴 P0 | P2 | 一次真实任务 Trace 详情（含 IC/IR） |
| 🔴 P0 | P7 | 前端模型选择 UI（4 个 phase 下拉） |
| 🟡 P1 | P2 | 回测结果指标面板 |
| 🟡 P1 | P6 | Trace 时间线详情页 |
| 🟡 P1 | P7 | 模型测试按钮 |
| 🟢 P2 | P2 | CoSTEER 演化终端日志 |

### 文案类
| 位置 | 内容 |
|---|---|
| P1 | 项目 logo / 启动时间 / 版本号（可选） |
| P8 | POC 客户/合作机构（无可删） |
| P9 | GitHub / 文档 / 联系方式 |
| P9 | 在投论文（无可留空） |

---

## 📖 所有引用的 docs/ 文档清单
- `docs/rdagent-cn.md`（项目总览中文）
- `docs/quickstart/Core.md`（核心流程）
- `docs/papers/README.md`（论文索引）
- `docs/papers/01-rdagent-framework.md`（RDLoop 总纲）
- `docs/papers/02-rdagent-quant.md`（量化双管线）
- `docs/papers/04-costeer.md`（CoSTEER）
- `docs/papers/05-multiagent-quant.md`（多智能体分工）
- `docs/papers/07-contextual-thompson-sampling.md`（LinTS）
- `docs/papers/benchmarks/B1~B5-*.md`（Benchmark GT 5 篇）
- 已有 PPT 可复用：[ppt/CoSTEER机制详解/CoSTEER机制详解.html](file:///workspace/ppt/CoSTEER机制详解/CoSTEER机制详解.html)

---

*提纲 v2 · 更新日期 2026-08-09*
*本次调整：(1) P1 加场景/智能体详表+多LLM映射；(2) P2 加截图清单；(3) P3 按 4 入口场景拆分；(4) P4 移走 token 图，加 B1~B5 还原率柱图；(5) P5 知识库紧跟 P4；(6) P7 多LLM 页放 token 饼图+成本解释，逻辑闭环*
